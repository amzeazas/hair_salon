<center>
  <h1>Hermoine's Magical Salon :: Current Stylists</h1>
  <h4>View the currently list of stylists working at the salon.</h4>
</center>
<h3>
<b><u>Stylists:</u><b>
  <% if @stylists.any? %>
  <ol>
    <% @stylists.each() do |stylist| %>
    <li><b><a href="/stylist/<%= stylist.id() %>"><%= stylist.last_name() %>, <%= stylist.first_name() %></a><br></b></li>
    <% end %>
  </ol>

  <% else %>
  <p>There are currently no stylists working at the salon.</p>
  <% end %>
<br>
<br>
<form action='/stylists' method="post">
  <div class="form-group">
    <label for="new_stylist">Add a new stylist:</label>
    <input id="first_name" name="first_name" class="form-control" type="text" placeholder="Add first name here.">
    <input id="last_name" name="last_name" class="form-control" type="text" placeholder="Add last name here.">
    <input id="phone_number" name="phone_number" class="form-control" type="text" placeholder="Add phone number here (e.g. 5038675309).">
  </div>
  <button type="submit" class="btn">Add Stylist</button>
</form>
</h3>
<br>
<a href='/'>Click here to return to the main salon page</a>
